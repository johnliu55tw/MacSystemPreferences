# Useful defaults configurations

## Disable the pop-up accent window when typing

```
# Disable
$ defaults write -g ApplePressAndHoldEnabled -bool false

# Enable
$ defaults write -g ApplePressAndHoldEnabled -bool true
```
